/**
 * Las funciones en programación es muy parecido al concepto de función matemática
 * 
 * f(x) => x^2
 * f(1) => 1^2 = 1
 * f(2) => 2^2 = 4
 */

/**
 * En JS YA estamos usando funciones
 */

// Funcion log , del objeto console
console.log("Hola!");

// Función del objeto Math random
console.log(Math.random());

// Otra función de JavaScript que ya estemos usando?
let youtubers = [];
youtubers.push("Manz");
console.log(youtubers);

let gatoMayusculas = "gato".toUpperCase();
console.log(gatoMayusculas + "!!!!!");