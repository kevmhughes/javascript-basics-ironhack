/**
 * Forma más cómoda de concatenar strings: template strings
 */

let edad = 5;
let nombre = "Loki";


// backticks ``
console.log(`Mi gato se llama ${nombre.toUpperCase()} y tiene ${edad} años.`);